# - 1 задача
#задаем первые числа фибоначи
#создаем цикл выполнения 100 раз
#1 число становится как 2, а оно в свою очередь равняется сумме а и b
#выводим одно из чисел последовательности

# - 2 задача
#вводим строку с параметрами
#забиваем масив
#забиваем текущую строку в которую и будем запсывать ответ
#забиваем переменную для выявления текущего числа
#цикл которых проходится по каждому символу в строке
#если наш символ является числом
#умножаем на 10 для сдвига влево для правильной обработки больших чисел( >10) иначе он будет считать их отдельно
#если наш символ является началом скобок
#к масиву добавляем текущую строку и наше число
#обнуляем строку до пустой
#обнуляем наше число
#если наш символ является концом скобок
#забиваем новые переменные для прошлой строки и числа которые равны последним сохраненным парам масива(текущей строке и текущему числу)
#складываем прошлую строку с текущей которая записывается количество раз которое забивается числом
#в других случаях
#добавляем к ответу просто наш символ
#выводим ответ

# - 3 задача
#вводим количество кустов
#вводим урожайность кустов через пробел
#забиваем переменную для подсчета суммы кустов для четных номеров в ряду
#забиваем переменную для подсчета суммы кустов для НЕчетных номеров в ряду
#создаем цикл выполнения n раз
#если первое число в ряду четное то:
#прибавляем к сумме четных
#если первое число в ряду НЕчетное то:
#прибавляем к сумме НЕчетных
#выводим максимальное из этих чисел
